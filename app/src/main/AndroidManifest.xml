<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyecto">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Proyecto">
        <activity android:name=".CerrarSesion"></activity>
        <activity android:name=".Chat" />
        <activity android:name=".DatosTrabajador" />
        <activity android:name=".PantallaPrincipal" />
        <activity android:name=".Marcaje.MainActivity" />
        <activity android:name=".Chat.MainActivity" />
        <activity android:name=".Chat.NuevoMensaje" />
        <activity android:name=".Chat.LatestMessage" />
        <activity android:name=".Chat.ChatLog" />
        <activity android:name=".RegistroUsuario.Registro" />
        <activity android:name=".RegistroUsuario.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>